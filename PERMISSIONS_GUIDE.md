# نظام الصلاحيات في تطبيق DineBuddies

## الوضع الحالي (مرحلة التطوير)

### 🔓 وضع التجربة (Demo Mode)
**الحالة**: مفعّل حالياً (`isDemoMode = true`)

**ماذا يعني هذا؟**
- ✅ يمكن لأي شخص تحرير أي بروفايل مطعم
- ✅ يظهر بانر أصفر في الأعلى يوضح أنك في وضع التجربة
- ✅ يمكنك التبديل بين أنواع الحسابات المختلفة للتجربة

**لماذا هذا مفيد الآن؟**
- للتطوير والاختبار
- لرؤية كيف ستبدو الواجهة لأنواع المستخدمين المختلفة
- لاختبار جميع الميزات بسهولة

---

## عند نشر التطبيق (الإنتاج)

### 🔒 وضع الإنتاج (Production Mode)
**الحالة**: سيتم تفعيله عند النشر (`isDemoMode = false`)

**ماذا سيحدث؟**

### 1. المستخدم العادي 👤
```
الاسم: ياسر (أنت حالياً)
الصلاحيات:
  ❌ لا يمكنه تحرير أي بروفايل مطعم
  ✅ يمكنه عرض البروفايلات
  ✅ يمكنه الانضمام للمجتمعات
  ✅ يمكنه إنشاء دعوات
```

### 2. مالك المنشأة 🏢
```
مثال: مدير Le Bistro Premium
الصلاحيات:
  ✅ يمكنه تحرير بروفايل مطعمه فقط (res_1)
  ❌ لا يمكنه تحرير مطاعم أخرى
  ✅ يمكنه عرض جميع البروفايلات
```

### 3. مدير النظام 👑
```
الصلاحيات:
  ✅ يمكنه تحرير أي بروفايل
  ✅ صلاحيات كاملة
```

---

## كيف تجرب الأنواع المختلفة؟

### في وضع التجربة الحالي:
1. افتح أي بروفايل شريك: `http://localhost:5173/partner/res_1`
2. ستجد بانر أصفر في الأعلى
3. اضغط على الأزرار للتبديل:
   - 👤 **مستخدم عادي**: لن يظهر زر التعديل
   - 🏢 **مالك منشأة**: سيظهر زر التعديل فقط على Le Bistro (res_1)
   - 👑 **مدير**: سيظهر زر التعديل على كل المطاعم

---

## للتبديل إلى وضع الإنتاج

### الخطوة 1: تعديل الكود
افتح الملف: `src/context/InvitationContext.jsx`

ابحث عن السطر (~57):
```javascript
const [isDemoMode, setIsDemoMode] = useState(true);
```

غيّره إلى:
```javascript
const [isDemoMode, setIsDemoMode] = useState(false);
```

### الخطوة 2: النتيجة
- ✅ اختفاء البانر الأصفر
- ✅ تطبيق نظام الصلاحيات الحقيقي
- ✅ المستخدمون العاديون لن يروا زر التعديل
- ✅ فقط أصحاب المطاعم يمكنهم تحرير مطاعمهم

---

## في التطبيق الحقيقي (مع Backend)

عند ربط التطبيق بـ Backend حقيقي:

### 1. تسجيل الدخول
```javascript
// سيتم التحقق من هوية المستخدم
POST /api/auth/login
{
  email: "owner@lebistro.sa",
  password: "***"
}

// الرد:
{
  user: {
    id: "partner_123",
    name: "مدير Le Bistro",
    userRole: "partner_owner",
    ownedRestaurants: ["res_1"]
  },
  token: "jwt_token_here"
}
```

### 2. التحقق من الصلاحيات
```javascript
// عند محاولة التعديل
PUT /api/restaurants/res_1
Headers: { Authorization: "Bearer jwt_token" }

// Backend يتحقق:
- هل المستخدم مسجل دخول؟
- هل هو مالك هذا المطعم؟
- أو هل هو مدير نظام؟
```

---

## الخلاصة

| الميزة | وضع التجربة (الآن) | وضع الإنتاج (عند النشر) |
|--------|-------------------|------------------------|
| **من يمكنه التعديل؟** | أي شخص | فقط المالك/المدير |
| **البانر الأصفر** | ✅ يظهر | ❌ لا يظهر |
| **تبديل الحسابات** | ✅ متاح | ❌ غير متاح |
| **الغرض** | التطوير والاختبار | الاستخدام الحقيقي |

**الآن**: أنت في وضع التجربة - يمكنك تحرير كل شيء للاختبار ✅
**لاحقاً**: عند النشر - فقط أصحاب المطاعم يمكنهم التحرير 🔒
