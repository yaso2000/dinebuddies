# **Special Offers in Partners Feed** 🔥

## **التعديلات التي تمت**

### ✅ **1. إضافة العروض الخاصة لصفحة Partners Feed**
تم إضافة قسم جديد في صفحة الشركاء (`Partners.jsx`) لعرض العروض الخاصة النشطة من الشركاء Premium.

### ✅ **2. إضافة العروض الخاصة لصفحة Home Feed**
تم إضافة قسم carousel أفقي في الصفحة الرئيسية (`Home.jsx`) لعرض العروض الخاصة للمستخدمين العاديين.

---

## **الميزات الجديدة**

### 📍 **عرض العروض في Home Feed (للمستخدمين العاديين)**
- يظهر carousel أفقي scrollable بعد البانر الإعلاني مباشرة
- يعرض العروض النشطة فقط (غير منتهية)
- يعرض العروض من الشركاء Premium فقط
- ترتيب العروض من الأحدث للأقدم
- تصميم مضغوط يناسب الموبايل

### 📍 **عرض العروض في Partners Feed**
- يظهر قسم "Special Offers" أعلى قائمة الشركاء
- يعرض العروض النشطة فقط (غير منتهية)
- يعرض العروض من الشركاء Premium فقط
- ترتيب العروض من الأحدث للأقدم

### 🎨 **التصميم**
- **كروت أفقية scrollable** بتصميم برتقالي ذهبي جذاب
- **HOT OFFER badge** في الزاوية العلوية اليمنى
- عرض صورة العرض (أو emoji 🎁 إذا لم توجد صورة)
- عرض نسبة الخصم بشكل بارز
- عرض اسم المنتج/الخدمة
- عرض اسم الشريك مع أيقونة
- **عداد الأيام المتبقية** للعرض
- تأثيرات hover جميلة

### 🔧 **الوظائف**
1. **جلب العروض النشطة**: دالة `fetchSpecialOffers()` تجلب العروض من Firestore
2. **فلترة العروض**: 
   - فقط العروض النشطة (`status === 'active'`)
   - فقط العروض غير منتهية الصلاحية
   - فقط من شركاء Premium
3. **النقر على العرض**: ينقل المستخدم لصفحة البروفايل الخاص بالشريك

---

## **أين تُعرض العروض الآن؟**

### ✅ **صفحة Home Feed** (جديد - للمستخدمين العاديين)
- المسار: `/` (الصفحة الرئيسية)
- يراها جميع المستخدمين العاديين
- عرض carousel أفقي مضغوط
- يظهر بعد البانر الإعلاني

### ✅ **صفحة Partners Feed** (جديد)
- المسار: `/partners`
- يراها جميع المستخدمين
- عرض أفقي scrollable

### ✅ **صفحة Partner Profile** (موجود مسبقاً)
- المسار: `/partners/:partnerId`
- عرض البانر الخاص بالشريك

---

## **ملخص التعديلات التقنية**

### **Home.jsx:**
```javascript
// Added imports
import { HiBuildingStorefront } from 'react-icons/hi2';
import { collection, query, where, getDocs, getDoc, doc } from 'firebase/firestore';
import { db } from '../firebase/config';

// Added states
const [specialOffers, setSpecialOffers] = useState([]);
const [loadingOffers, setLoadingOffers] = useState(true);

// Added useEffect to fetch offers
useEffect(() => { fetchSpecialOffers(); }, []);
```

### **Partners.jsx:**
```javascript
// Same imports and states as Home.jsx
// Same fetch logic
```

---

## **المستقبل**

يمكن لاحقاً إضافة:
- صفحة منفصلة للعروض فقط
- إشعارات push للعروض الجديدة
- فلتر لعرض العروض حسب الفئة
- إحصائيات للنقرات على العروض

---

## **ملاحظات تقنية**

- العروض تُجلب مرة واحدة عند تحميل الصفحة
- يمكن إضافة real-time updates باستخدام `onSnapshot` بدلاً من `getDocs`
- الصور تُحمل من Firebase Storage
- التواريخ تُعالج باستخدام Firestore Timestamps
- التصميم responsive ويعمل على جميع الأحجام

---

✨ **جاهز للاستخدام في صفحتين!**

