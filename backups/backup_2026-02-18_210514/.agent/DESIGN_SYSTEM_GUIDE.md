# 🎨 DineBuddies Design System - Color Palette Guide

## ✅ التحديثات المنفذة

تم تنفيذ نظام ألوان محسّن ومنظم في **2026-02-09**

---

## 📋 ملخص التحسينات

### ✅ التحسينات الرئيسية:

1. **حذف المتغيرات المكررة**
   - ❌ حذف `--text-primary` (كانت نفس `--text-main`)
   - ❌ حذف `--bg-secondary` (استبدلت بـ `--bg-elevated`)
   - ❌ حذف `--btn-primary-bg` (استخدم `--primary` مباشرة)
   - ❌ حذف `--btn-secondary-bg` (استخدم `--secondary` مباشرة)

2. **إضافة نظام ألوان Semantic**
   - ✅ `--color-success` (أخضر)
   - ✅ `--color-error` / `--color-danger` (أحمر)
   - ✅ `--color-warning` (برتقالي)
   - ✅ `--color-info` (أزرق فاتح)

3. **إضافة درجات الألوان (50-900)**
   - ✅ Primary (بنفسجي): `--primary-50` إلى `--primary-900`
   - ✅ Secondary (وردي): `--secondary-50` إلى `--secondary-900`
   - ✅ Gold (ذهبي): `--gold-50` إلى `--gold-900`
   - ✅ Success (أخضر): `--success-50` إلى `--success-900`
   - ✅ Error (أحمر): `--error-50` إلى `--error-900`
   - ✅ Warning (برتقالي): `--warning-50` إلى `--warning-900`
   - ✅ Info (أزرق): `--info-50` إلى `--info-900`

4. **إضافات جديدة**
   - ✅ `--bg-elevated` (للعناصر المرتفعة)
   - ✅ `--bg-hover` (حالة التمرير)
   - ✅ `--text-disabled` (نص معطل)
   - ✅ `--border-color-strong` و `--border-color-subtle`
   - ✅ درجات الظلال (sm, md, lg, xl)
   - ✅ نظام Spacing (xs → 2xl)
   - ✅ Transitions (fast, base, slow)
   - ✅ Orange و Gray utilities

---

## 🎨 دليل الاستخدام

### الألوان الرئيسية (Brand Colors)

#### Primary - البنفسجي
```css
var(--primary)        /* #8b5cf6 - الاستخدام الأساسي */
var(--primary-hover)  /* #7c3aed - عند التمرير */
var(--primary-500)    /* نفس primary */
var(--primary-600)    /* نفس primary-hover */

/* درجات أخرى */
var(--primary-300)    /* فاتح جداً */
var(--primary-700)    /* غامق */
```

**الاستخدام:**
- الأزرار الرئيسية
- الروابط النشطة
- العناصر المميزة
- التظليل والتوهج

#### Secondary - الوردي/الأحمر
```css
var(--secondary)      /* #f43f5e */
```

**الاستخدام:**
- الأزرار الثانوية
- التنبيهات المهمة
- العناصر المميزة البديلة

#### Luxury Gold - الذهبي
```css
var(--luxury-gold)    /* #fbbf24 */
```

**الاستخدام:**
- علامات Premium
- نجوم التقييم
- العملات والأسعار
- العناصر الفاخرة

---

### الألوان الدلالية (Semantic Colors)

#### Success - النجاح
```css
var(--color-success)  /* #10b981 */
```

**استخدم لـ:**
- رسائل النجاح
- حالة "تم" / "مكتمل"
- زر الموافقة
- أيقونات الصح ✓

#### Error / Danger - الخطأ
```css
var(--color-error)    /* #ef4444 */
var(--color-danger)   /* نفس error */
```

**استخدم لـ:**
- رسائل الخطأ
- التحذيرات الخطيرة
- أزرار الحذف
- الحالات الرافضة

#### Warning - التحذير
```css
var(--color-warning)  /* #f59e0b */
```

**استخدم لـ:**
- تحذيرات متوسطة
- حالات "انتبه"
- حد الاقتراب من الحد الأقصى

#### Info - المعلومات
```css
var(--color-info)     /* #06b6d4 */
```

**استخدم لـ:**
- رسائل معلوماتية
- نصائح
- توجيهات

---

### الخلفيات (Backgrounds)

```css
var(--bg-body)        /* #020617 - الأغمق - خلفية الصفحة */
var(--bg-card)        /* #0f172a - غامق - الكروت والمودال */
var(--bg-elevated)    /* #1e293b - متوسط - عناصر مرفوعة */
var(--bg-input)       /* #1e293b - حقول الإدخال */
var(--bg-hover)       /* #334155 - حالة التمرير */
```

**القاعدة:**
- استخدم `bg-body` للصفحة الرئيسية
- استخدم `bg-card` للكروت والبانلز
- استخدم `bg-elevated` للعناصر فوق الكروت
- استخدم `bg-hover` لحالة hover

---

### النصوص (Text)

```css
var(--text-main)      /* #f8fafc - نص رئيسي (الأفتح) */
var(--text-secondary) /* #cbd5e1 - نص ثانوي */
var(--text-muted)     /* #94a3b8 - نص خافت */
var(--text-disabled)  /* #64748b - نص معطل */
var(--text-white)     /* #ffffff - أبيض نقي */
```

**القاعدة:**
- استخدم `text-main` للعناوين والنصوص المهمة
- استخدم `text-secondary` للنصوص الفرعية
- استخدم `text-muted` للتفاصيل الثانوية
- استخدم `text-disabled` للعناصر المعطلة

---

### الحدود (Borders)

```css
var(--border-color)        /* rgba(255,255,255,0.1) - عادي */
var(--border-color-strong) /* rgba(255,255,255,0.2) - قوي */
var(--border-color-subtle) /* rgba(255,255,255,0.05) - خفيف */
```

---

### Border Radius

```css
var(--radius-sm)   /* 12px - صغير */
var(--radius-md)   /* 18px - متوسط */
var(--radius-lg)   /* 24px - كبير */
var(--radius-xl)   /* 32px - كبير جداً */
var(--radius-full) /* 9999px - دائري كامل */
```

---

### الظلال (Shadows)

```css
var(--shadow-sm)       /* ظل صغير */
var(--shadow-md)       /* ظل متوسط */
var(--shadow-lg)       /* ظل كبير */
var(--shadow-xl)       /* ظل كبير جداً */
var(--shadow-premium)  /* ظل فاخر */
var(--shadow-glow)     /* توهج بنفسجي */
var(--shadow-glow-gold)/* توهج ذهبي */
```

---

### المسافات (Spacing)

```css
var(--space-xs)   /* 4px */
var(--space-sm)   /* 8px */
var(--space-md)   /* 16px */
var(--space-lg)   /* 24px */
var(--space-xl)   /* 32px */
var(--space-2xl)  /* 48px */
```

---

### الانتقالات (Transitions)

```css
var(--transition-fast)  /* 150ms - سريع */
var(--transition-base)  /* 200ms - عادي */
var(--transition-slow)  /* 300ms - بطيء */
```

**مثال:**
```css
.button {
  transition: all var(--transition-base);
}
```

---

## 📊 أمثلة عملية

### مثال 1: زر أساسي
```css
.btn-primary {
  background: var(--primary);
  color: var(--btn-text);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-glow);
  transition: all var(--transition-base);
}

.btn-primary:hover {
  background: var(--primary-hover);
}
```

### مثال 2: رسالة نجاح
```css
.alert-success {
  background: var(--success-900);
  border: 1px solid var(--success-600);
  color: var(--success-50);
  border-radius: var(--radius-sm);
  padding: var(--space-md);
}
```

### مثال 3: كرت
```css
.card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg);
  padding: var(--space-lg);
  box-shadow: var(--shadow-md);
}

.card:hover {
  background: var(--bg-elevated);
  border-color: var(--border-color-strong);
}
```

---

## 🎯 قواعد الاستخدام

### ✅ افعل:
- استخدم متغيرات CSS بدلاً من الألوان المباشرة
- استخدم الألوان الدلالية للحالات (success, error, etc.)
- اتبع نظام التدرج (50-900) عند الحاجة لدرجات مختلفة
- استخدم spacing scale للمسافات

### ❌ لا تفعل:
- لا تكتب ألوان hex مباشرة في الكود
- لا تستخدم `--accent` (مهمل - استخدم `--color-success`)
- لا تخلط بين `--primary` و `--primary-500` (نفس القيمة)
- لا تستخدم قيم spacing عشوائية (استخدم scale)

---

## 🔄 Migration Guide (للكود القديم)

إذا وجدت في الكود القديم:
```css
/* ❌ قديم */
color: #8b5cf6;
background: #f43f5e;
border: 1px solid rgba(255,255,255,0.1);
padding: 16px;
```

غيّره إلى:
```css
/* ✅ جديد */
color: var(--primary);
background: var(--secondary);
border: 1px solid var(--border-color);
padding: var(--space-md);
```

---

## 📝 ملاحظات مهمة

1. **--accent مهمل**: استخدم `--color-success` بدلاً منه
2. **--bg-secondary محذوف**: استخدم `--bg-elevated` بدلاً منه
3. **--text-primary محذوف**: استخدم `--text-main` بدلاً منه
4. **الأزرار**: لم تعد هناك حاجة لـ `--btn-primary-bg`، استخدم `--primary` مباشرة

---

## 🎨 الخطوات القادمة

- [ ] استبدال hard-coded colors في الملفات (عمل تدريجي)
- [ ] إنشاء components مع الألوان الجديدة
- [ ] تحديث التوثيق لكل component
- [ ] إضافة أمثلة في Storybook (مستقبلاً)
