# 🔧 حل مشكلة النشر على Vercel

## ❌ المشكلة:
```
sh: Permission denied
Error: Command "npm run build" exited with 126
```

## ✅ الحل:

### الخطوة 1: إعدادات المشروع في Vercel

اذهب إلى صفحة المشروع في Vercel:
1. اضغط على **Settings** (الإعدادات)
2. اختر **General** من القائمة الجانبية
3. ابحث عن قسم **Build & Development Settings**

### الخطوة 2: اضبط الإعدادات التالية:

#### Framework Preset:
```
Vite
```

#### Build Command:
اضغط **Override** واكتب:
```
npm run build
```

#### Output Directory:
اضغط **Override** واكتب:
```
dist
```

#### Install Command (الأهم!):
اضغط **Override** واكتب:
```
npm install --legacy-peer-deps
```

### الخطوة 3: احفظ الإعدادات
اضغط **Save** في أسفل الصفحة

### الخطوة 4: إعادة النشر
1. اذهب إلى **Deployments**
2. اضغط على النقاط الثلاث `...` بجانب آخر deployment
3. اختر **Redeploy**
4. انتظر 1-2 دقيقة

---

## 🎯 لماذا حدثت المشكلة؟

- `vercel.json` لا يدعم `buildCommand` و `installCommand` بهذا الشكل
- هذه الإعدادات يجب أن تكون في لوحة تحكم Vercel
- تم إزالتها من `vercel.json` ورفع التحديث إلى GitHub

---

## ✅ بعد النشر:

يجب أن ترى:
```
✓ Installing dependencies
✓ Building...
✓ Deployment ready
```

وستحصل على رابط مثل:
```
https://dinebuddies.vercel.app
```

---

## 🆘 إذا استمرت المشكلة:

أرسل لي screenshot من:
1. صفحة Settings → Build & Development Settings
2. آخر Build Logs

وسأساعدك في الحل!
