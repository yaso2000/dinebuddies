# Dating invitation templates (source)

Copy of assets synced into `public/dating-invitation-templates/` by:

```bash
npm run sync:dating-templates
```

Place webp/jpg/png files in `backgrounds/` using the **template id** as the filename stem (see `src/components/Invitations/datingCard/datingCardBackgrounds.js`).
