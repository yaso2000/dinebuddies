# ⚙️ إعدادات Vercel للنشر

## 🎯 الإعدادات المطلوبة:

### 1. Framework Preset
```
Vite
```

### 2. Build Command
```
npm run build
```

### 3. Output Directory
```
dist
```

### 4. Install Command (مهم جداً!)
```
npm install --legacy-peer-deps
```

**⚠️ ملاحظة مهمة:**
يجب تفعيل "Override" بجانب Install Command وكتابة الأمر أعلاه لحل مشكلة تعارض React.

---

## 📋 خطوات النشر بالتفصيل:

### الخطوة 1: فتح Vercel
1. اذهب إلى: https://vercel.com/new
2. سجل دخول بحساب GitHub

### الخطوة 2: استيراد المشروع
1. ستظهر قائمة بـ repositories
2. ابحث عن: `dinebuddies` أو `yaso2000/dinebuddies`
3. اضغط **"Import"**

### الخطوة 3: الإعدادات
1. **Project Name**: اتركه كما هو أو غيّره
2. **Framework Preset**: اختر **Vite**
3. **Root Directory**: اتركه `./`
4. **Build and Output Settings**:
   - Build Command: `npm run build` ✅
   - Output Directory: `dist` ✅
   - Install Command: اضغط **"Override"** واكتب:
     ```
     npm install --legacy-peer-deps
     ```

### الخطوة 4: النشر
1. اضغط **"Deploy"**
2. انتظر 1-2 دقيقة
3. ستحصل على رابط مثل: `https://dinebuddies.vercel.app`

---

## 🔧 حل المشاكل:

### مشكلة 1: Build Failed - React Conflict
**الخطأ:**
```
npm ERR! Could not resolve dependency:
npm ERR! peer react@"^19.0.0" from react-leaflet@5.0.0
```

**الحل:**
1. اذهب إلى Project Settings → General
2. في Build & Development Settings
3. غيّر Install Command إلى:
   ```
   npm install --legacy-peer-deps
   ```
4. احفظ واضغط Redeploy

### مشكلة 2: صفحة 404 عند التنقل
**الحل:**
تأكد من وجود ملف `vercel.json` في المشروع مع المحتوى:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### مشكلة 3: صفحة فارغة
**الحل:**
1. افتح Console (F12)
2. ابحث عن أخطاء JavaScript
3. تأكد من Output Directory = `dist`

---

## ✅ التحقق من النشر:

بعد النشر، اختبر:
- ✅ الصفحة الرئيسية تفتح
- ✅ يمكن إنشاء دعوة جديدة
- ✅ التنقل بين الصفحات يعمل
- ✅ تعدد اللغات يعمل
- ✅ الخريطة تظهر
- ✅ الصور تحمّل

---

## 🔄 إعادة النشر بعد التعديلات:

عندما تعدل الكود:
1. احفظ التغييرات
2. ارفع إلى GitHub:
   ```cmd
   git add .
   git commit -m "Update features"
   git push origin main
   ```
3. Vercel سيعيد النشر تلقائياً!

---

## 📊 مراقبة الأداء:

في لوحة تحكم Vercel:
- **Analytics**: عدد الزوار
- **Logs**: سجل الأخطاء
- **Deployments**: تاريخ النشر

---

## 🎉 تهانينا!
بمجرد النشر، تطبيقك سيكون حياً على الإنترنت!
