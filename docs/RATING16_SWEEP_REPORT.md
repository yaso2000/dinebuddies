# Rating 16+ copy sweep report

Generated automatically. Locale keys changed or removed (removed keys fall back to English via i18next `fallbackLng`).

| locale | key | old | new |
|---|---|---|---|
| en | `admin_invitations_lead` | Filter by type (public / private / dating) — delete, block, or republish. | Filter by type (public / private) — delete, block, or republish. |
| en | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | Seed consumer profiles with photos and local coordinates. No business accounts. |
| en | `ai_text_starter_first_date` | How do I stay safe on a first date? | How do I stay safe when meeting someone new? |
| en | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | Tips for social meet-ups and table etiquette, grounded in research-backed sources. |
| en | `ai_prompt_private_default` | Write a private invite title and a short romantic message suited to the occasion. | Write a warm, friendly invitation title and a short message suited to the occasion. |
| en | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | You need to follow each other to chat. |
| en | `chat_relationship_dating` | Dating | Friends |
| en | `community_chat_zone_theme_romantic` | Romantic red | Warm red |
| en | `discovery_match_dating_title` | Dating match! | You're now friends! |
| en | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | You and {{name}} now follow each other |
| en | `profile_open_to_dating_title` | Open to dating | (removed) |
| en | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed) |
| en | `social_type_serious` | Serious relationship | Getting acquainted |
| en | `dating_setup_title` | Dating profile | (removed) |
| en | `dating_setup_desc` | Your dating profile is separate from your social profile and only appears in the dating space. | (removed) |
| en | `dating_deck_empty` | No more dating profiles right now — check back later. | (removed) |
| en | `dating_consent` | I am 18+ and accept the dating terms: no objectionable content or harassment. | (removed) |
| en | `dating_enable_cta` | Enable dating | (removed) |
| en | `dating_need_consent` | Please accept the dating terms. | (removed) |
| en | `dating_enabled_ok` | Dating is on. Your profile is live in the dating space. | (removed) |
| en | `dating_required_match` | Enable dating first to use Match or Not. | (removed) |
| en | `dating_settings_entry` | Dating | (removed) |
| en | `invite_create_private_title` | Dating | Private invite |
| en | `join_reason_open_to_dating` | I'm open to dating 💕 | (removed) |
| en | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | Your profile is now hidden from member discovery. |
| en | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | Your profile is hidden from member discovery. |
| en | `personal_invite_badge` | Dating | Personal invite |
| en | `personal_invite_cat_dating` | Dating | Getting acquainted |
| en | `personal_invite_cat_dating_desc` | One-on-one with romantic potential. | A relaxed one-on-one meal to get to know someone. |
| en | `personal_invite_cat_serious` | Serious relationship | Getting acquainted |
| en | `personal_invite_title` | Dating invite | Personal invite |
| en | `social_available` | Available for Dating | Available for private invites |
| en | `social_available_disable` | Available for Dating — click to disable | Available for private invites — click to disable |
| en | `social_cover_section_hint` | Camera, upload from your device, or a ready template from the dating library. | Camera, upload from your device, or a ready template from the library. |
| en | `social_grad_decor_label` | Romantic overlay | Warm overlay |
| en | `social_tpl_golden_love_night` | Golden love night | Golden night |
| en | `social_tpl_heart_roses` | Heart-shaped roses | Roses |
| en | `social_tpl_love_in_bloom` | Love in bloom | In bloom |
| en | `social_tpl_romantic_coffee_escape` | Romantic coffee escape | Coffee escape |
| en | `social_tpl_sunset_romance` | Sunset romance | Sunset glow |
| en | `social_unavailable` | Not available for Dating | Not available for private invites |
| en | `social_unavailable_enable` | Not available for Dating — click to enable | Not available for private invites — click to enable |
| en | `gift_rose_desc` | A classic romantic gesture | A classic friendly gesture |
| en | `gift_rose_preview` | A classic romantic gesture 🌹 | A classic friendly gesture 🌹 |
| ar | `admin_invitations_lead` | فرز حسب النوع (عام / خاص / خاصة) — حذف، حجب، أو إعادة نشر. | تصفية حسب النوع (عام / خاص) — حذف أو حظر أو إعادة نشر. |
| ar | `admin_demo_users_lead` | إنشاء بروفايلات مستهلكين بصور ومواعدة مفتوحة وموقع جغرافي. بدون حسابات أعمال. | إنشاء ملفات مستخدمين تجريبية بصور وإحداثيات محلية. بدون حسابات أعمال. |
| ar | `ai_text_starter_first_date` | كيف أبقى آمناً في الموعد الأول؟ | كيف أبقى آمناً عند لقاء شخص جديد؟ |
| ar | `ai_text_studio_subtitle` | نصائح للمواعدة والعلاقات مبنية على مصادر بحثية موثوقة. | نصائح للقاءات الاجتماعية وآداب المائدة مبنية على مصادر موثوقة. |
| ar | `ai_prompt_private_default` | اكتب عنوان دعوة خاصة ورسالة رومانسية قصيرة ومناسبة | اكتب عنواناً ودوداً للدعوة ورسالة قصيرة مناسبة للمناسبة. |
| ar | `chat_connection_locked` | تحتاجان اتصالاً متبادلاً للدردشة — مواعدة، صداقة، أو تعارف. | تحتاجان إلى متابعة بعضكما لبدء الدردشة. |
| ar | `chat_relationship_dating` | مواعدة | أصدقاء |
| ar | `community_chat_zone_theme_romantic` | رومانسي أحمر | أحمر دافئ |
| ar | `discovery_match_dating_title` | تطابق للمواعدة! | أصبحتما صديقين! |
| ar | `discovery_match_dating_subtitle` | أنت و{{name}} تواصلتما للمواعدة | أنت و{{name}} تتابعان بعضكما الآن |
| ar | `profile_open_to_dating_title` | منفتح/ة للمواعدة | (removed) |
| ar | `profile_open_to_dating_hint` | عند التفعيل يظهر قلب (مواعدة) فقط إذا كان الطرفان منفتحين للمواعدة. إذا رفض أحدكما المواعدة تظهر أيقونة المتابعة. | (removed) |
| ar | `social_type_serious` | علاقة جدية | تعارف |
| ar | `dating_setup_title` | ملف المواعدة | (removed) |
| ar | `dating_setup_desc` | ملف المواعدة منفصل عن ملفك الاجتماعي ويظهر فقط في فضاء المواعدة. | (removed) |
| ar | `dating_deck_empty` | لا مزيد من بروفايلات المواعدة الآن — عُد لاحقًا. | (removed) |
| ar | `dating_consent` | عمري 18+ وأوافق على شروط المواعدة: لا محتوى مسيء ولا تحرّش. | (removed) |
| ar | `dating_enable_cta` | تفعيل المواعدة | (removed) |
| ar | `dating_need_consent` | يرجى الموافقة على شروط المواعدة. | (removed) |
| ar | `dating_enabled_ok` | تم تفعيل المواعدة. ملفك ظاهر الآن في فضاء المواعدة. | (removed) |
| ar | `dating_required_match` | فعّل المواعدة أولًا لاستخدام «ماتش أو لا؟». | (removed) |
| ar | `dating_settings_entry` | المواعدة | (removed) |
| ar | `invite_create_private_title` | مواعدة | دعوة خاصة |
| ar | `join_reason_open_to_dating` | أنا منفتح على المواعدة 💕 | (removed) |
| ar | `profile_dating_hidden_from_lists_toast` | لن يظهر ملفك بعد الآن في قوائم الأعضاء واكتشاف المواعدة. | أصبح ملفك مخفياً من اكتشاف الأعضاء. |
| ar | `profile_dating_hidden_removed_toast` | تم إخفاء ملفك من قوائم المواعدة، وأُزيل خيار «مواعدة» من تفضيلاتك. | ملفك مخفي من اكتشاف الأعضاء. |
| ar | `personal_invite_badge` | مواعدة | دعوة شخصية |
| ar | `personal_invite_cat_dating` | مواعدة | تعارف |
| ar | `personal_invite_cat_dating_desc` | لقاء ثنائي بإمكانية أقرب. | وجبة هادئة بين شخصين للتعرف على بعضكما. |
| ar | `personal_invite_cat_serious` | علاقة جدية | تعارف |
| ar | `personal_invite_title` | دعوة مواعدة | دعوة شخصية |
| ar | `social_available` | متاح للخاصة | متاح للدعوات الخاصة |
| ar | `social_available_disable` | متاح للخاصة — انقر للإلغاء | متاح للدعوات الخاصة — اضغط للتعطيل |
| ar | `social_cover_section_hint` | الكاميرا، رفع من جهازك، أو قالب جاهز من مكتبة دعوة خاصة. | الكاميرا، أو رفع من جهازك، أو قالب جاهز من المكتبة. |
| ar | `social_grad_decor_label` | زخرفة رومانسية | طبقة دافئة |
| ar | `social_tpl_golden_love_night` | ليلة حب ذهبية | ليلة ذهبية |
| ar | `social_tpl_heart_roses` | قلب من الورود | ورود |
| ar | `social_tpl_love_in_bloom` | حب في ازدهار | تفتّح |
| ar | `social_tpl_romantic_coffee_escape` | مهرب قهوة رومانسي | هروب إلى القهوة |
| ar | `social_tpl_sunset_romance` | رومانسية الغروب | وهج الغروب |
| ar | `social_unavailable` | غير متاح للخاصة | غير متاح للدعوات الخاصة |
| ar | `social_unavailable_enable` | غير متاح للخاصة — انقر للتفعيل | غير متاح للدعوات الخاصة — اضغط للتفعيل |
| ar | `gift_rose_desc` | لمسة رومانسية كلاسيكية | لفتة ودّية كلاسيكية |
| ar | `gift_rose_preview` | لمسة رومانسية كلاسيكية 🌹 | لفتة ودّية كلاسيكية 🌹 |
| de | `admin_invitations_lead` | Nach Typ filtern (öffentlich / privat / Dating) — löschen, blockieren oder erneut veröffentlichen. | (removed → falls back to English) |
| de | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| de | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| de | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| de | `ai_prompt_private_default` | Schreibe einen Dating-Einladungstitel und eine kurze romantische Nachricht passend zum Anlass. | (removed → falls back to English) |
| de | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| de | `chat_relationship_dating` | Dating | (removed → falls back to English) |
| de | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| de | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| de | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| de | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| de | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| de | `social_type_serious` | Ernste Beziehung | (removed → falls back to English) |
| de | `dating_setup_title` | Dating-Profil | (removed → falls back to English) |
| de | `dating_setup_desc` | Dein Dating-Profil ist von deinem sozialen Profil getrennt und erscheint nur im Dating-Bereich. | (removed → falls back to English) |
| de | `dating_deck_empty` | Gerade keine weiteren Dating-Profile – schau später wieder vorbei. | (removed → falls back to English) |
| de | `dating_consent` | Ich bin 18+ und akzeptiere die Dating-Bedingungen: keine anstößigen Inhalte oder Belästigung. | (removed → falls back to English) |
| de | `dating_enable_cta` | Dating aktivieren | (removed → falls back to English) |
| de | `dating_need_consent` | Bitte akzeptiere die Dating-Bedingungen. | (removed → falls back to English) |
| de | `dating_enabled_ok` | Dating ist an. Dein Profil ist im Dating-Bereich live. | (removed → falls back to English) |
| de | `dating_required_match` | Aktiviere zuerst Dating, um Match oder nicht zu nutzen. | (removed → falls back to English) |
| de | `dating_settings_entry` | Dating | (removed → falls back to English) |
| de | `invite_create_private_title` | Private Einladung | (removed → falls back to English) |
| de | `join_reason_open_to_dating` | Ich bin offen für Dating 💕 | (removed → falls back to English) |
| de | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| de | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| de | `personal_invite_badge` | Persönliche Einladung | (removed → falls back to English) |
| de | `personal_invite_cat_dating` | Dating | (removed → falls back to English) |
| de | `personal_invite_cat_dating_desc` | Eins zu eins mit romantischem Potenzial. | (removed → falls back to English) |
| de | `personal_invite_cat_serious` | Ernste Beziehung | (removed → falls back to English) |
| de | `personal_invite_title` | Persönliche Einladung | (removed → falls back to English) |
| de | `social_available` | Für Dating verfügbar | (removed → falls back to English) |
| de | `social_available_disable` | Für Dating verfügbar — zum Deaktivieren klicken | (removed → falls back to English) |
| de | `social_cover_section_hint` | Kamera, Upload vom Gerät oder eine fertige Vorlage aus der Dating-Bibliothek. | (removed → falls back to English) |
| de | `social_grad_decor_label` | Romantisches Overlay | (removed → falls back to English) |
| de | `social_tpl_golden_love_night` | Goldene Liebesnacht | (removed → falls back to English) |
| de | `social_tpl_heart_roses` | Herzförmige Rosen | (removed → falls back to English) |
| de | `social_tpl_love_in_bloom` | Liebe in Blüte | (removed → falls back to English) |
| de | `social_tpl_romantic_coffee_escape` | Romantische Kaffee-Auszeit | (removed → falls back to English) |
| de | `social_tpl_sunset_romance` | Sonnenuntergangs-Romantik | (removed → falls back to English) |
| de | `social_unavailable` | Nicht für Dating verfügbar | (removed → falls back to English) |
| de | `social_unavailable_enable` | Nicht für Dating verfügbar — zum Aktivieren klicken | (removed → falls back to English) |
| de | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| de | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| es | `admin_invitations_lead` | Filtrar por tipo (pública / privada / citas) — eliminar, bloquear o republicar. | (removed → falls back to English) |
| es | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| es | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| es | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| es | `ai_prompt_private_default` | Escribe un título de invitación de cita y un mensaje romántico breve acorde a la ocasión. | (removed → falls back to English) |
| es | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| es | `chat_relationship_dating` | Citas | (removed → falls back to English) |
| es | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| es | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| es | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| es | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| es | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| es | `social_type_serious` | Relación seria | (removed → falls back to English) |
| es | `dating_setup_title` | Perfil de citas | (removed → falls back to English) |
| es | `dating_setup_desc` | Tu perfil de citas es independiente de tu perfil social y solo aparece en el espacio de citas. | (removed → falls back to English) |
| es | `dating_deck_empty` | No hay más perfiles de citas ahora mismo — vuelve más tarde. | (removed → falls back to English) |
| es | `dating_consent` | Tengo 18 años o más y acepto los términos de citas: sin contenido objetable ni acoso. | (removed → falls back to English) |
| es | `dating_enable_cta` | Activar citas | (removed → falls back to English) |
| es | `dating_need_consent` | Por favor, acepta los términos de citas. | (removed → falls back to English) |
| es | `dating_enabled_ok` | Las citas están activadas. Tu perfil está en vivo en el espacio de citas. | (removed → falls back to English) |
| es | `dating_required_match` | Activa las citas primero para usar ¿Pareja o no?. | (removed → falls back to English) |
| es | `dating_settings_entry` | Citas | (removed → falls back to English) |
| es | `invite_create_private_title` | Invitación privada | (removed → falls back to English) |
| es | `join_reason_open_to_dating` | Estoy abierto/a a citas 💕 | (removed → falls back to English) |
| es | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| es | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| es | `personal_invite_badge` | Invitación personal | (removed → falls back to English) |
| es | `personal_invite_cat_dating` | Citas | (removed → falls back to English) |
| es | `personal_invite_cat_dating_desc` | Uno a uno con potencial romántico. | (removed → falls back to English) |
| es | `personal_invite_cat_serious` | Relación seria | (removed → falls back to English) |
| es | `personal_invite_title` | Invitación personal | (removed → falls back to English) |
| es | `social_available` | Disponible para citas | (removed → falls back to English) |
| es | `social_available_disable` | Disponible para citas — haz clic para desactivar | (removed → falls back to English) |
| es | `social_cover_section_hint` | Cámara, subir desde tu dispositivo o una plantilla lista de la biblioteca de citas. | (removed → falls back to English) |
| es | `social_grad_decor_label` | Superposición romántica | (removed → falls back to English) |
| es | `social_tpl_golden_love_night` | Noche de amor dorada | (removed → falls back to English) |
| es | `social_tpl_heart_roses` | Rosas en forma de corazón | (removed → falls back to English) |
| es | `social_tpl_love_in_bloom` | Amor en flor | (removed → falls back to English) |
| es | `social_tpl_romantic_coffee_escape` | Escape romántico al café | (removed → falls back to English) |
| es | `social_tpl_sunset_romance` | Romance al atardecer | (removed → falls back to English) |
| es | `social_unavailable` | No disponible para citas | (removed → falls back to English) |
| es | `social_unavailable_enable` | No disponible para citas — haz clic para activar | (removed → falls back to English) |
| es | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| es | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| fr | `admin_invitations_lead` | Filtrer par type (publique / privée / rencontre) — supprimer, bloquer ou republier. | (removed → falls back to English) |
| fr | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| fr | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| fr | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| fr | `ai_prompt_private_default` | Rédigez un titre et un message romantique pour un rendez-vous. | (removed → falls back to English) |
| fr | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| fr | `chat_relationship_dating` | Rencontre | (removed → falls back to English) |
| fr | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| fr | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| fr | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| fr | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| fr | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| fr | `social_type_serious` | Relation sérieuse | (removed → falls back to English) |
| fr | `dating_setup_title` | Profil de rencontre | (removed → falls back to English) |
| fr | `dating_setup_desc` | Votre profil de rencontre est distinct de votre profil social et n'apparaît que dans l'espace rencontre. | (removed → falls back to English) |
| fr | `dating_deck_empty` | Plus de profils de rencontre pour le moment — revenez plus tard. | (removed → falls back to English) |
| fr | `dating_consent` | J'ai 18 ans ou plus et j'accepte les conditions de rencontre : aucun contenu répréhensible ni harcèlement. | (removed → falls back to English) |
| fr | `dating_enable_cta` | Activer les rencontres | (removed → falls back to English) |
| fr | `dating_need_consent` | Veuillez accepter les conditions de rencontre. | (removed → falls back to English) |
| fr | `dating_enabled_ok` | Les rencontres sont activées. Votre profil est en ligne dans l'espace rencontre. | (removed → falls back to English) |
| fr | `dating_required_match` | Activez d'abord les rencontres pour utiliser Match ou pas. | (removed → falls back to English) |
| fr | `dating_settings_entry` | Rencontres | (removed → falls back to English) |
| fr | `invite_create_private_title` | Invitation privée | (removed → falls back to English) |
| fr | `join_reason_open_to_dating` | Je suis ouvert(e) aux rencontres 💕 | (removed → falls back to English) |
| fr | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| fr | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| fr | `personal_invite_badge` | Invitation personnelle | (removed → falls back to English) |
| fr | `personal_invite_cat_dating` | Rencontres | (removed → falls back to English) |
| fr | `personal_invite_cat_dating_desc` | En tête-à-tête avec un potentiel romantique. | (removed → falls back to English) |
| fr | `personal_invite_cat_serious` | Relation sérieuse | (removed → falls back to English) |
| fr | `personal_invite_title` | Invitation personnelle | (removed → falls back to English) |
| fr | `social_available` | Disponible pour les rendez-vous | (removed → falls back to English) |
| fr | `social_available_disable` | Disponible pour les rendez-vous — cliquez pour désactiver | (removed → falls back to English) |
| fr | `social_cover_section_hint` | Caméra, téléversement depuis votre appareil ou un modèle prêt de la bibliothèque rendez-vous. | (removed → falls back to English) |
| fr | `social_grad_decor_label` | Superposition romantique | (removed → falls back to English) |
| fr | `social_tpl_golden_love_night` | Nuit d'amour dorée | (removed → falls back to English) |
| fr | `social_tpl_heart_roses` | Roses en forme de cœur | (removed → falls back to English) |
| fr | `social_tpl_love_in_bloom` | Amour en fleurs | (removed → falls back to English) |
| fr | `social_tpl_romantic_coffee_escape` | Échappée café romantique | (removed → falls back to English) |
| fr | `social_tpl_sunset_romance` | Romance au coucher du soleil | (removed → falls back to English) |
| fr | `social_unavailable` | Non disponible pour les rendez-vous | (removed → falls back to English) |
| fr | `social_unavailable_enable` | Non disponible pour les rendez-vous — cliquez pour activer | (removed → falls back to English) |
| fr | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| fr | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| hi | `admin_invitations_lead` | प्रकार के आधार पर फ़िल्टर करें (सार्वजनिक / निजी / डेटिंग) — हटाएँ, ब्लॉक करें या फिर से प्रकाशित करें। | (removed → falls back to English) |
| hi | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| hi | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| hi | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| hi | `ai_prompt_private_default` | इस अवसर के लिए उपयुक्त एक निजी आमंत्रण शीर्षक और एक छोटा रोमांटिक संदेश लिखें। | (removed → falls back to English) |
| hi | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| hi | `chat_relationship_dating` | डेटिंग | (removed → falls back to English) |
| hi | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| hi | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| hi | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| hi | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| hi | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| hi | `social_type_serious` | गंभीर रिश्ता | (removed → falls back to English) |
| hi | `dating_setup_title` | डेटिंग प्रोफ़ाइल | (removed → falls back to English) |
| hi | `dating_setup_desc` | आपकी डेटिंग प्रोफ़ाइल आपकी सामाजिक प्रोफ़ाइल से अलग है और केवल डेटिंग स्पेस में दिखती है। | (removed → falls back to English) |
| hi | `dating_deck_empty` | अभी और कोई डेटिंग प्रोफ़ाइल नहीं — बाद में देखें। | (removed → falls back to English) |
| hi | `dating_consent` | मैं 18+ हूँ और डेटिंग शर्तें स्वीकार करता हूँ: कोई आपत्तिजनक सामग्री या उत्पीड़न नहीं। | (removed → falls back to English) |
| hi | `dating_enable_cta` | डेटिंग सक्षम करें | (removed → falls back to English) |
| hi | `dating_need_consent` | कृपया डेटिंग शर्तें स्वीकार करें। | (removed → falls back to English) |
| hi | `dating_enabled_ok` | डेटिंग चालू है। आपकी प्रोफ़ाइल डेटिंग स्पेस में लाइव है। | (removed → falls back to English) |
| hi | `dating_required_match` | मैच या नहीं उपयोग करने के लिए पहले डेटिंग सक्षम करें। | (removed → falls back to English) |
| hi | `dating_settings_entry` | डेटिंग | (removed → falls back to English) |
| hi | `invite_create_private_title` | व्यक्तिगत आमंत्रण | (removed → falls back to English) |
| hi | `join_reason_open_to_dating` | मैं डेटिंग के लिए तैयार हूं 💕 | (removed → falls back to English) |
| hi | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| hi | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| hi | `personal_invite_badge` | व्यक्तिगत आमंत्रण | (removed → falls back to English) |
| hi | `personal_invite_cat_dating` | डेटिंग | (removed → falls back to English) |
| hi | `personal_invite_cat_dating_desc` | रोमांटिक क्षमता वाले एक-पर-एक। | (removed → falls back to English) |
| hi | `personal_invite_cat_serious` | गंभीर रिश्ता | (removed → falls back to English) |
| hi | `personal_invite_title` | व्यक्तिगत आमंत्रण | (removed → falls back to English) |
| hi | `social_available` | डेटिंग के लिए उपलब्ध | (removed → falls back to English) |
| hi | `social_available_disable` | डेटिंग के लिए उपलब्ध - अक्षम करने के लिए क्लिक करें | (removed → falls back to English) |
| hi | `social_cover_section_hint` | कैमरा, अपने डिवाइस से अपलोड करें, या डेटिंग लाइब्रेरी से तैयार टेम्पलेट। | (removed → falls back to English) |
| hi | `social_grad_decor_label` | रोमांटिक ओवरले | (removed → falls back to English) |
| hi | `social_tpl_golden_love_night` | सुनहरी प्रेम रात्रि | (removed → falls back to English) |
| hi | `social_tpl_heart_roses` | दिल के आकार के गुलाब | (removed → falls back to English) |
| hi | `social_tpl_love_in_bloom` | प्रेम खिल रहा है | (removed → falls back to English) |
| hi | `social_tpl_romantic_coffee_escape` | रोमांटिक कॉफ़ी से बचना | (removed → falls back to English) |
| hi | `social_tpl_sunset_romance` | सूर्यास्त रोमांस | (removed → falls back to English) |
| hi | `social_unavailable` | डेटिंग के लिए उपलब्ध नहीं है | (removed → falls back to English) |
| hi | `social_unavailable_enable` | डेटिंग के लिए उपलब्ध नहीं - सक्षम करने के लिए क्लिक करें | (removed → falls back to English) |
| hi | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| hi | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| it | `admin_invitations_lead` | Filtra per tipo (pubblico / privato / appuntamenti) — elimina, blocca o ripubblica. | (removed → falls back to English) |
| it | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| it | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| it | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| it | `ai_prompt_private_default` | Scrivi un titolo per un invito appuntamento e un breve messaggio romantico adatto all'occasione. | (removed → falls back to English) |
| it | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| it | `chat_relationship_dating` | Appuntamenti | (removed → falls back to English) |
| it | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| it | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| it | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| it | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| it | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| it | `social_type_serious` | Relazione seria | (removed → falls back to English) |
| it | `dating_setup_title` | Profilo di incontri | (removed → falls back to English) |
| it | `dating_setup_desc` | Il tuo profilo di incontri è separato dal tuo profilo social e appare solo nello spazio incontri. | (removed → falls back to English) |
| it | `dating_deck_empty` | Nessun altro profilo di incontri al momento — ricontrolla più tardi. | (removed → falls back to English) |
| it | `dating_consent` | Ho 18+ anni e accetto i termini degli incontri: nessun contenuto discutibile o molestia. | (removed → falls back to English) |
| it | `dating_enable_cta` | Attiva incontri | (removed → falls back to English) |
| it | `dating_need_consent` | Accetta i termini degli incontri. | (removed → falls back to English) |
| it | `dating_enabled_ok` | Gli incontri sono attivi. Il tuo profilo è online nello spazio incontri. | (removed → falls back to English) |
| it | `dating_required_match` | Attiva prima gli incontri per usare Match o no. | (removed → falls back to English) |
| it | `dating_settings_entry` | Incontri | (removed → falls back to English) |
| it | `invite_create_private_title` | Invito privato | (removed → falls back to English) |
| it | `join_reason_open_to_dating` | Sono aperto/a agli appuntamenti 💕 | (removed → falls back to English) |
| it | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| it | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| it | `personal_invite_badge` | Invito personale | (removed → falls back to English) |
| it | `personal_invite_cat_dating` | Appuntamenti | (removed → falls back to English) |
| it | `personal_invite_cat_dating_desc` | Uno a uno con potenziale romantico. | (removed → falls back to English) |
| it | `personal_invite_cat_serious` | Relazione seria | (removed → falls back to English) |
| it | `personal_invite_title` | Invito personale | (removed → falls back to English) |
| it | `social_available` | Disponibile per appuntamenti | (removed → falls back to English) |
| it | `social_available_disable` | Disponibile per appuntamenti — clicca per disattivare | (removed → falls back to English) |
| it | `social_cover_section_hint` | Fotocamera, caricamento dal dispositivo o un modello pronto dalla libreria appuntamenti. | (removed → falls back to English) |
| it | `social_grad_decor_label` | Sovrapposizione romantica | (removed → falls back to English) |
| it | `social_tpl_golden_love_night` | Notte d'amore dorata | (removed → falls back to English) |
| it | `social_tpl_heart_roses` | Rose a forma di cuore | (removed → falls back to English) |
| it | `social_tpl_love_in_bloom` | Amore in fiore | (removed → falls back to English) |
| it | `social_tpl_romantic_coffee_escape` | Fuga romantica al caffè | (removed → falls back to English) |
| it | `social_tpl_sunset_romance` | Romanticismo al tramonto | (removed → falls back to English) |
| it | `social_unavailable` | Non disponibile per appuntamenti | (removed → falls back to English) |
| it | `social_unavailable_enable` | Non disponibile per appuntamenti — clicca per attivare | (removed → falls back to English) |
| it | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| it | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| pt | `admin_invitations_lead` | Filtrar por tipo (público / privado / encontros) — excluir, bloquear ou republicar. | (removed → falls back to English) |
| pt | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| pt | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| pt | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| pt | `ai_prompt_private_default` | Escreva um título de convite para encontro e uma mensagem romântica curta adequada à ocasião. | (removed → falls back to English) |
| pt | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| pt | `chat_relationship_dating` | Namoro | (removed → falls back to English) |
| pt | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| pt | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| pt | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| pt | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| pt | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| pt | `social_type_serious` | Relacionamento sério | (removed → falls back to English) |
| pt | `dating_setup_title` | Perfil de namoro | (removed → falls back to English) |
| pt | `dating_setup_desc` | Seu perfil de namoro é separado do seu perfil social e só aparece no espaço de namoro. | (removed → falls back to English) |
| pt | `dating_deck_empty` | Nenhum perfil de namoro no momento — volte mais tarde. | (removed → falls back to English) |
| pt | `dating_consent` | Tenho 18+ e aceito os termos de namoro: sem conteúdo censurável ou assédio. | (removed → falls back to English) |
| pt | `dating_enable_cta` | Ativar namoro | (removed → falls back to English) |
| pt | `dating_need_consent` | Aceite os termos de namoro. | (removed → falls back to English) |
| pt | `dating_enabled_ok` | O namoro está ativado. Seu perfil está no ar no espaço de namoro. | (removed → falls back to English) |
| pt | `dating_required_match` | Ative o namoro primeiro para usar Combina ou Não. | (removed → falls back to English) |
| pt | `dating_settings_entry` | Namoro | (removed → falls back to English) |
| pt | `invite_create_private_title` | Convite privado | (removed → falls back to English) |
| pt | `join_reason_open_to_dating` | Estou aberto(a) a encontros 💕 | (removed → falls back to English) |
| pt | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| pt | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| pt | `personal_invite_badge` | Convite pessoal | (removed → falls back to English) |
| pt | `personal_invite_cat_dating` | Encontros | (removed → falls back to English) |
| pt | `personal_invite_cat_dating_desc` | Um a um com potencial romântico. | (removed → falls back to English) |
| pt | `personal_invite_cat_serious` | Relacionamento sério | (removed → falls back to English) |
| pt | `personal_invite_title` | Convite pessoal | (removed → falls back to English) |
| pt | `social_available` | Disponível para encontros | (removed → falls back to English) |
| pt | `social_available_disable` | Disponível para encontros — clique para desativar | (removed → falls back to English) |
| pt | `social_cover_section_hint` | Câmera, envio do seu dispositivo ou um modelo pronto da biblioteca de encontros. | (removed → falls back to English) |
| pt | `social_grad_decor_label` | Sobreposição romântica | (removed → falls back to English) |
| pt | `social_tpl_golden_love_night` | Noite de amor dourada | (removed → falls back to English) |
| pt | `social_tpl_heart_roses` | Rosas em forma de coração | (removed → falls back to English) |
| pt | `social_tpl_love_in_bloom` | Amor florescendo | (removed → falls back to English) |
| pt | `social_tpl_romantic_coffee_escape` | Escape romântico ao café | (removed → falls back to English) |
| pt | `social_tpl_sunset_romance` | Romance ao pôr do sol | (removed → falls back to English) |
| pt | `social_unavailable` | Indisponível para encontros | (removed → falls back to English) |
| pt | `social_unavailable_enable` | Indisponível para encontros — clique para ativar | (removed → falls back to English) |
| pt | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| pt | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| tr | `admin_invitations_lead` | Türe göre filtrele (herkese açık / özel / flört) — sil, engelle veya yeniden yayınla. | (removed → falls back to English) |
| tr | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| tr | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| tr | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| tr | `ai_prompt_private_default` | Etkinliğe uygun bir flört davetiyesi başlığı ve kısa romantik mesaj yaz. | (removed → falls back to English) |
| tr | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| tr | `chat_relationship_dating` | Flört | (removed → falls back to English) |
| tr | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| tr | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| tr | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| tr | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| tr | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| tr | `social_type_serious` | Ciddi ilişki | (removed → falls back to English) |
| tr | `dating_setup_title` | Flört profili | (removed → falls back to English) |
| tr | `dating_setup_desc` | Flört profilin sosyal profilinden ayrıdır ve yalnızca flört alanında görünür. | (removed → falls back to English) |
| tr | `dating_deck_empty` | Şu an başka flört profili yok — sonra tekrar bak. | (removed → falls back to English) |
| tr | `dating_consent` | 18 yaşından büyüğüm ve flört koşullarını kabul ediyorum: uygunsuz içerik veya taciz yok. | (removed → falls back to English) |
| tr | `dating_enable_cta` | Flörtü etkinleştir | (removed → falls back to English) |
| tr | `dating_need_consent` | Lütfen flört koşullarını kabul et. | (removed → falls back to English) |
| tr | `dating_enabled_ok` | Flört açık. Profilin flört alanında yayında. | (removed → falls back to English) |
| tr | `dating_required_match` | Eşleşme mi Değil mi'yi kullanmak için önce flörtü etkinleştir. | (removed → falls back to English) |
| tr | `dating_settings_entry` | Flört | (removed → falls back to English) |
| tr | `invite_create_private_title` | Özel davetiye | (removed → falls back to English) |
| tr | `join_reason_open_to_dating` | Flörte açığım 💕 | (removed → falls back to English) |
| tr | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| tr | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| tr | `personal_invite_badge` | Kişisel davet | (removed → falls back to English) |
| tr | `personal_invite_cat_dating` | Flört | (removed → falls back to English) |
| tr | `personal_invite_cat_dating_desc` | Romantik potansiyeli olan bire bir. | (removed → falls back to English) |
| tr | `personal_invite_cat_serious` | Ciddi ilişki | (removed → falls back to English) |
| tr | `personal_invite_title` | Kişisel davet | (removed → falls back to English) |
| tr | `social_available` | Randevu İçin Uygun | (removed → falls back to English) |
| tr | `social_available_disable` | Randevu İçin Uygun — kapatmak için tıkla | (removed → falls back to English) |
| tr | `social_cover_section_hint` | Kamera, cihazından yükleme veya randevu kütüphanesinden hazır şablon. | (removed → falls back to English) |
| tr | `social_grad_decor_label` | Romantik kaplama | (removed → falls back to English) |
| tr | `social_tpl_golden_love_night` | Altın aşk gecesi | (removed → falls back to English) |
| tr | `social_tpl_heart_roses` | Kalp şeklinde güller | (removed → falls back to English) |
| tr | `social_tpl_love_in_bloom` | Çiçek açan aşk | (removed → falls back to English) |
| tr | `social_tpl_romantic_coffee_escape` | Romantik kahve kaçamağı | (removed → falls back to English) |
| tr | `social_tpl_sunset_romance` | Gün batımı romantizmi | (removed → falls back to English) |
| tr | `social_unavailable` | Randevu İçin Uygun Değil | (removed → falls back to English) |
| tr | `social_unavailable_enable` | Randevu İçin Uygun Değil — açmak için tıkla | (removed → falls back to English) |
| tr | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| tr | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| ur | `admin_invitations_lead` | قسم کے لحاظ سے فلٹر کریں (عوامی / نجی / ڈیٹنگ) — حذف کریں، بلاک کریں یا دوبارہ شائع کریں۔ | (removed → falls back to English) |
| ur | `admin_demo_users_lead` | Seed consumer profiles with photos, dating open, and local coordinates. No business accounts. | (removed → falls back to English) |
| ur | `ai_text_starter_first_date` | How do I stay safe on a first date? | (removed → falls back to English) |
| ur | `ai_text_studio_subtitle` | Dating and relationship advice grounded in research-backed sources. | (removed → falls back to English) |
| ur | `ai_prompt_private_default` | ایک نجی دعوت نامے کا عنوان اور اس موقع کے لیے موزوں ایک مختصر رومانوی پیغام لکھیں۔ | (removed → falls back to English) |
| ur | `chat_connection_locked` | You need a mutual connection to chat — dating match, friendship, or acquaintance. | (removed → falls back to English) |
| ur | `chat_relationship_dating` | ڈیٹنگ | (removed → falls back to English) |
| ur | `community_chat_zone_theme_romantic` | Romantic red | (removed → falls back to English) |
| ur | `discovery_match_dating_title` | Dating match! | (removed → falls back to English) |
| ur | `discovery_match_dating_subtitle` | You and {{name}} connected for dating | (removed → falls back to English) |
| ur | `profile_open_to_dating_title` | Open to dating | (removed → falls back to English) |
| ur | `profile_open_to_dating_hint` | When on, a heart (dating) appears only if you both accept dating. If either person is not open to dating, they'll see fo | (removed → falls back to English) |
| ur | `social_type_serious` | سنجیدہ تعلق | (removed → falls back to English) |
| ur | `dating_setup_title` | ڈیٹنگ پروفائل | (removed → falls back to English) |
| ur | `dating_setup_desc` | آپ کی ڈیٹنگ پروفائل آپ کی سماجی پروفائل سے الگ ہے اور صرف ڈیٹنگ اسپیس میں ظاہر ہوتی ہے۔ | (removed → falls back to English) |
| ur | `dating_deck_empty` | ابھی مزید ڈیٹنگ پروفائلز نہیں — بعد میں دوبارہ دیکھیں۔ | (removed → falls back to English) |
| ur | `dating_consent` | میں 18+ ہوں اور ڈیٹنگ شرائط قبول کرتا ہوں: کوئی قابلِ اعتراض مواد یا ہراسانی نہیں۔ | (removed → falls back to English) |
| ur | `dating_enable_cta` | ڈیٹنگ فعال کریں | (removed → falls back to English) |
| ur | `dating_need_consent` | براہ کرم ڈیٹنگ شرائط قبول کریں۔ | (removed → falls back to English) |
| ur | `dating_enabled_ok` | ڈیٹنگ فعال ہے۔ آپ کی پروفائل ڈیٹنگ اسپیس میں لائیو ہے۔ | (removed → falls back to English) |
| ur | `dating_required_match` | میچ یا نہیں استعمال کرنے کے لیے پہلے ڈیٹنگ فعال کریں۔ | (removed → falls back to English) |
| ur | `dating_settings_entry` | ڈیٹنگ | (removed → falls back to English) |
| ur | `invite_create_private_title` | ذاتی دعوت | (removed → falls back to English) |
| ur | `join_reason_open_to_dating` | میں ڈیٹنگ کے لیے تیار ہوں💕 | (removed → falls back to English) |
| ur | `profile_dating_hidden_from_lists_toast` | Your profile is now hidden from member discovery and dating lists. | (removed → falls back to English) |
| ur | `profile_dating_hidden_removed_toast` | Your profile is hidden from dating discovery lists, and Dating was removed from your preferences. | (removed → falls back to English) |
| ur | `personal_invite_badge` | ذاتی دعوت | (removed → falls back to English) |
| ur | `personal_invite_cat_dating` | ڈیٹنگ | (removed → falls back to English) |
| ur | `personal_invite_cat_dating_desc` | رومانوی صلاحیت کے ساتھ ون آن ون۔ | (removed → falls back to English) |
| ur | `personal_invite_cat_serious` | سنجیدہ تعلق | (removed → falls back to English) |
| ur | `personal_invite_title` | ذاتی دعوت | (removed → falls back to English) |
| ur | `social_available` | ڈیٹنگ کے لیے دستیاب ہے۔ | (removed → falls back to English) |
| ur | `social_available_disable` | ڈیٹنگ کے لیے دستیاب — غیر فعال کرنے کے لیے کلک کریں۔ | (removed → falls back to English) |
| ur | `social_cover_section_hint` | کیمرہ، اپنے آلے سے اپ لوڈ، یا ڈیٹنگ لائبریری سے تیار ٹیمپلیٹ۔ | (removed → falls back to English) |
| ur | `social_grad_decor_label` | رومانٹک اوورلے | (removed → falls back to English) |
| ur | `social_tpl_golden_love_night` | سنہری محبت کی رات | (removed → falls back to English) |
| ur | `social_tpl_heart_roses` | دل کی شکل کے گلاب | (removed → falls back to English) |
| ur | `social_tpl_love_in_bloom` | محبت کھلتی ہے۔ | (removed → falls back to English) |
| ur | `social_tpl_romantic_coffee_escape` | رومانٹک کافی فرار | (removed → falls back to English) |
| ur | `social_tpl_sunset_romance` | غروب آفتاب کا رومانس | (removed → falls back to English) |
| ur | `social_unavailable` | ڈیٹنگ کے لیے دستیاب نہیں ہے۔ | (removed → falls back to English) |
| ur | `social_unavailable_enable` | ڈیٹنگ کے لیے دستیاب نہیں — فعال کرنے کے لیے کلک کریں۔ | (removed → falls back to English) |
| ur | `gift_rose_desc` | A classic romantic gesture | (removed → falls back to English) |
| ur | `gift_rose_preview` | A classic romantic gesture 🌹 | (removed → falls back to English) |
| en | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | Deep burgundy and rose — warm and rich. |
| en | `private_pair_accepted_dating` | Date confirmed | Meet-up confirmed |
| en | `private_pair_aria_dating` | Date pairing | Meet-up pairing |
| en | `private_pair_heading_dating` | Your date | Your meet-up |
| ar | `community_chat_zone_theme_romantic_desc` | عنابي غامق ووردي — دافئ وعميق. | عنابي غامق ووردي — دافئ وعميق. |
| ar | `private_pair_accepted_dating` | تم تأكيد الموعد | تم تأكيد اللقاء |
| ar | `private_pair_aria_dating` | اقتران الموعد | اقتران اللقاء |
| ar | `private_pair_heading_dating` | موعدك | لقاؤك |
| de | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| de | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| de | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| de | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| es | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| es | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| es | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| es | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| fr | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| fr | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| fr | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| fr | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| hi | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| hi | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| hi | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| hi | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| it | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| it | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| it | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| it | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| pt | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| pt | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| pt | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| pt | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| tr | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| tr | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| tr | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| tr | `private_pair_heading_dating` | Your date | (removed → falls back to English) |
| ur | `community_chat_zone_theme_romantic_desc` | Deep burgundy and rose — intimate and rich. | (removed → falls back to English) |
| ur | `private_pair_accepted_dating` | Date confirmed | (removed → falls back to English) |
| ur | `private_pair_aria_dating` | Date pairing | (removed → falls back to English) |
| ur | `private_pair_heading_dating` | Your date | (removed → falls back to English) |

## Code-side text changes

- `aiPromptLimits.js`: private-invite AI prompt and text-assistant starter no longer romantic / first-date.
- `personalityTraits.js`: trait `romantic` → `warm` (🌹 kept).
- `profileGifts.js`: rose gift copy → friendly gesture.
- `MatchCelebrationOverlay.jsx`, `Chat.jsx`: legacy `dating` kind renders as friendship (no heart icon).
- `aiDesignStudioUseDestinations.js`: private-invite cover icon 💑 → 🍽️.
- `PrivateProfileFields.jsx` / `Profile.jsx`: 'open to dating' switch and lock removed; `openToDating.js`, `datingToggleLock.js` deleted.
- `connectConnection.js`: single friendship connection (mutual follow); tests updated.
- `personalInviteCategories.js`, `socialInviteTypes.js`, `socialCardOccasionMap.js`, `socialCardBackgrounds.js`: 'serious' category removed; legacy values map to 'acquaintance'; `public/invitation-card-backgrounds/serious/` deleted.
- `ageCategories.js` (new): 16-17 bucket, `isMinorUser`; used by CompleteProfile, ProfileCompletionModal, invitation age groups, demo tools.
- `PrivacyPolicy.jsx`: rewritten sections (company, precise location, purchases, AI, deletion/reporting, 16–17, dates).
| en | `suitability_create_desc` | Share your card and let the crowd pick which partner type suits you best. It stays live on the stories rail for 24 hours | Share your card and let the crowd pick which table-buddy type suits you best. It stays live on the stories rail for 24 h |
| en | `suitability_pick_prompt` | Which partner type suits them best? | Which table-buddy type suits them best? |
| ar | `suitability_create_desc` | انشر بطاقتك ودع الجمهور يختار نوع الشريك الأنسب لك. تبقى على شريط الستوري ٢٤ ساعة. | انشر بطاقتك ودع الجمهور يختار نوع رفيق المائدة الأنسب لك. تبقى على شريط الستوري ٢٤ ساعة. |
| ar | `suitability_pick_prompt` | أي نوع شريك يناسبه أكثر؟ | أي نوع رفيق مائدة يناسبه أكثر؟ |
| de | `suitability_create_desc` | Teile deine Karte und lass die Menge wählen, welcher Partnertyp am besten zu dir passt. Sie bleibt 24 Stunden lang in de | (removed → falls back to English) |
| de | `suitability_pick_prompt` | Welcher Partnertyp passt am besten zu ihnen? | (removed → falls back to English) |
| es | `suitability_create_desc` | Comparte tu tarjeta y deja que la gente elija qué tipo de pareja te conviene más. Permanece en vivo en el carrusel de hi | (removed → falls back to English) |
| es | `suitability_pick_prompt` | ¿Qué tipo de pareja le conviene más? | (removed → falls back to English) |
| fr | `suitability_create_desc` | Partagez votre carte et laissez la foule choisir quel type de partenaire vous correspond le mieux. Elle reste en ligne s | (removed → falls back to English) |
| fr | `suitability_pick_prompt` | Quel type de partenaire lui correspond le mieux ? | (removed → falls back to English) |
| hi | `suitability_create_desc` | अपना कार्ड शेयर करें और लोगों को चुनने दें कि किस तरह का साथी आपके लिए सबसे सही है। यह 24 घंटे तक स्टोरीज़ रेल पर लाइव र | (removed → falls back to English) |
| hi | `suitability_pick_prompt` | उनके लिए कौन सा साथी प्रकार सबसे सही है? | (removed → falls back to English) |
| it | `suitability_create_desc` | Condividi la tua card e lascia che la folla scelga quale tipo di partner ti si addice di più. Resta online sulla barra d | (removed → falls back to English) |
| it | `suitability_pick_prompt` | Quale tipo di partner gli si addice di più? | (removed → falls back to English) |
| pt | `suitability_create_desc` | Compartilhe seu cartão e deixe a galera escolher qual tipo de parceiro combina mais com você. Fica ao vivo no rail de st | (removed → falls back to English) |
| pt | `suitability_pick_prompt` | Qual tipo de parceiro combina mais com ela? | (removed → falls back to English) |
| tr | `suitability_create_desc` | Kartını paylaş ve kalabalık sana en çok hangi partner tipinin yakıştığını seçsin. Hikâyeler barında 24 saat canlı kalır. | (removed → falls back to English) |
| tr | `suitability_pick_prompt` | Ona en çok hangi partner tipi yakışır? | (removed → falls back to English) |
| ur | `suitability_create_desc` | اپنا کارڈ شیئر کریں اور لوگوں کو منتخب کرنے دیں کہ کس قسم کا ساتھی آپ کے لیے سب سے موزوں ہے۔ یہ اسٹوریز ریل پر 24 گھنٹے  | (removed → falls back to English) |
| ur | `suitability_pick_prompt` | کس قسم کا ساتھی ان کے لیے سب سے موزوں ہے؟ | (removed → falls back to English) |
- `suitabilityArchetypes.js`: 'partner archetypes' reframed as table-buddy archetypes; 'The Romantic' → 'The Expressive' (id kept).

## Dead keys removed from all locales (old Dating space / Match-or-Not show, no longer referenced in code)

`dating_toggle_locked_message`, `match_create_desc2`, `match_your_profile`, `match_want_gender`, `match_empty_slot`, `match_host_tag`, `match_host_pick_hint2`, `match_wait_hint2`, `match_next2`, `match_bring_up`, `match_apply_hint2`, `match_age`, `match_goal`, `match_goal_marriage`, `match_goal_longterm`, `match_goal_shortterm`, `match_goal_undecided`, `match_interests`, `match_interests_ph`, `match_lookingfor`, `match_lookingfor_ph`, `match_about`, `match_about_ph`, `match_ai`, `match_ai_wait`, `match_ai_failed`, `match_age_invalid`, `match_need_looking`, `match_title`, `match_voice_label`, `match_voice_record`, `match_voice_stop`, `match_voice_uploading`, `match_voice_recorded`, `match_voice_play`, `match_voice_failed`, `match_create_title`, `match_create_desc`, `match_create_cta`, `match_create_error`, `match_slots`, `match_slot_a`, `match_slot_b`, `match_not_found`, `match_ended`, `match_host_pick_hint`, `match_wait_hint`, `match_yes`, `match_no`, `match_vote_yes`, `match_vote_no`, `match_you_on_stage`, `match_reveal`, `match_next`, `match_end`, `match_apply`, `match_apply_hint`, `match_in_queue`, `match_withdraw`, `match_queue`, `match_queue_empty`, `match_pick_second`, `match_voice`, `match_text`, `match_record`, `match_stop`, `match_rerecord`, `match_record_first`, `match_write_first`, `match_too_long`, `match_words`, `match_text_ph`, `match_submit`, `match_applied`, `match_mic_denied`, `dating_photo_hint`, `dating_use_current_photo`, `dating_nickname`, `dating_nickname_ph`, `dating_age_category`, `dating_gender`, `dating_lookingfor`, `dating_its_a_match`, `dating_match`, `dating_matches_title`, `dating_no_matches`, `dating_chat_start`, `dating_skip`, `dating_like`, `dating_need_lookingfor`, `dating_saving`, `dating_need_photo`, `dating_need_nickname`, `dating_need_age`, `dating_need_gender`, `dating_save_error`, `dating_settings_enabled`, `dating_settings_disabled`

## Additional copy

| locale | key | old | new |
|---|---|---|---|
| en | `social_tpl_sweetheart_rooftop` | Sweetheart rooftop | Rooftop evening |
| en | `group_game_top_pair` | Couple of the night | Most in sync |
| en | `group_game_taste_desc` | Everyone answers fun this-or-that questions. See who is most in sync — and the couple of the night. Open to everyone. | Everyone answers fun this-or-that questions. See who is most in sync at the table. Open to everyone. |
| ar | `social_tpl_sweetheart_rooftop` | سطح الحبيب | أمسية على السطح |
| ar | `group_game_top_pair` | ثنائي الليلة | الأكثر انسجاماً |
| ar | `group_game_taste_desc` | يجيب الجميع عن أسئلة «هذا أم ذاك» الممتعة. اكتشفوا الأكثر توافقًا — وثنائي الليلة. مفتوحة للجميع. | يجيب الجميع عن أسئلة «هذا أم ذاك» الممتعة. اكتشفوا الأكثر انسجاماً على المائدة. مفتوحة للجميع. |
| de | `social_tpl_sweetheart_rooftop` | Dachterrasse der Herzen | (removed → falls back to English) |
| de | `group_game_top_pair` | Paar des Abends | (removed → falls back to English) |
| de | `group_game_taste_desc` | Alle beantworten lustige Entweder-oder-Fragen. Seht, wer am meisten im Einklang ist – und das Paar des Abends. Offen für | (removed → falls back to English) |
| es | `social_tpl_sweetheart_rooftop` | Azotea romántica | (removed → falls back to English) |
| es | `group_game_top_pair` | Pareja de la noche | (removed → falls back to English) |
| es | `group_game_taste_desc` | Todos responden divertidas preguntas de esto-o-aquello. Descubre quién está más en sintonía — y la pareja de la noche. A | (removed → falls back to English) |
| fr | `social_tpl_sweetheart_rooftop` | Toit des amoureux | (removed → falls back to English) |
| fr | `group_game_top_pair` | Couple de la soirée | (removed → falls back to English) |
| fr | `group_game_taste_desc` | Tout le monde répond à des questions amusantes ceci ou cela. Découvrez qui est le plus en phase — et le couple de la soi | (removed → falls back to English) |
| hi | `social_tpl_sweetheart_rooftop` | जानेमन छत | (removed → falls back to English) |
| hi | `group_game_top_pair` | रात का जोड़ा | (removed → falls back to English) |
| hi | `group_game_taste_desc` | हर कोई मज़ेदार यह-या-वह प्रश्नों के जवाब देता है। देखें कौन सबसे ज़्यादा तालमेल में है — और रात का जोड़ा। सभी के लिए खुल | (removed → falls back to English) |
| it | `social_tpl_sweetheart_rooftop` | Rooftop romantico | (removed → falls back to English) |
| it | `group_game_top_pair` | Coppia della serata | (removed → falls back to English) |
| it | `group_game_taste_desc` | Tutti rispondono a divertenti domande questo-o-quello. Scopri chi è più in sintonia — e la coppia della serata. Aperto a | (removed → falls back to English) |
| pt | `social_tpl_sweetheart_rooftop` | Terraço dos apaixonados | (removed → falls back to English) |
| pt | `group_game_top_pair` | Casal da noite | (removed → falls back to English) |
| pt | `group_game_taste_desc` | Todos respondem perguntas divertidas de isto-ou-aquilo. Veja quem está mais em sintonia — e o casal da noite. Aberto a t | (removed → falls back to English) |
| tr | `social_tpl_sweetheart_rooftop` | Sevgili terası | (removed → falls back to English) |
| tr | `group_game_top_pair` | Gecenin çifti | (removed → falls back to English) |
| tr | `group_game_taste_desc` | Herkes eğlenceli şu-mu-bu sorularını yanıtlar. Kimin en uyumlu olduğunu ve gecenin çiftini gör. Herkese açık. | (removed → falls back to English) |
| ur | `social_tpl_sweetheart_rooftop` | پیاری چھت | (removed → falls back to English) |
| ur | `group_game_top_pair` | رات کا جوڑا | (removed → falls back to English) |
| ur | `group_game_taste_desc` | سب دلچسپ یہ-یا-وہ سوالات کے جواب دیتے ہیں۔ دیکھیں کون سب سے زیادہ ہم آہنگ ہے — اور رات کا جوڑا۔ سب کے لیے کھلا۔ | (removed → falls back to English) |

## Implemented in this pass (17 Sept 2026)

- Age categories: new `src/constants/ageCategories.js` (adds `16-17`, `isMinorUser`, `categoryFromAge`); wired into `CompleteProfile.jsx`, `ProfileCompletionModal.jsx`, invitation age-group lists, demo tools, `UserProfile.jsx`; `functions/demoUsersCore.js` accepts `16-17`.
- Dating logic removed: `openToDating.js`, `datingToggleLock.js` (+tests) deleted; `connectConnection.js` is follow-only (tests rewritten); `Profile.jsx`, `PrivateProfileFields.jsx`, `userDirectory.js`, `defaultProfileMedia.js`, `discoveryProfile.js`, `DemoUsersPage.jsx`, `LookingForChips.jsx`, `Chat.jsx`, `MatchCelebrationOverlay.jsx` cleaned; `functions/index.js` + `connectMatchNotifications.js` return friendship only.
- "Serious relationship" removed from `personalInviteCategories.js`, `socialInviteTypes.js`, `socialCardOccasionMap.js`, `socialCardBackgrounds.js`; legacy values → "Getting acquainted"; `public/invitation-card-backgrounds/serious/` deleted.
- Minor safety (16–17 ⟷ adults): `src/utils/minorSafety.js` (+tests) — applied in discovery swipe (`discoverySwipeMatch.js`), member directory (`useUserDirectory.js`, `UsersDirectory.jsx`, `useDiscoveryProfiles.js`), suggested friends, direct-chat gate (`connectConnection.isConnectionComplete`), private-invite invitee check (`CreatePrivateInvitation.jsx`); server-side in `firestore.rules` (`sameAgeClass` on `conversations` create and single-invitee `social_invitations` create).
- Legal pages: `PrivacyPolicy.jsx` rewritten sections; `TermsOfService.jsx` minimum age 16 + 16–17 protections; dates → September 2026.
- Pick One: legacy singers comment/folder removed.

## Still to do (owner)

- Replace all invitation card backgrounds (cartoon/3D, no humans). File names unchanged.
- Run `npm test` and `npm run build` on Windows (the Linux mount cannot run the Windows `esbuild`/`vitest` binaries), then `npx cap sync`.
- Deploy `firestore.rules` and Cloud Functions (`firebase deploy --only firestore:rules,functions`).
- Store metadata + age-rating questionnaires (Apple + Google).
