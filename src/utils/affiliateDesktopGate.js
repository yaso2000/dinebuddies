/**
 * Affiliate dashboard is allowed on all viewports (mobile included for sharing links).
 * Helpers kept for compatibility with older imports.
 */
export function isAffiliateDesktopShell() {
    return true;
}

export function useAffiliateDesktopEligible() {
    return true;
}
