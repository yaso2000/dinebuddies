/**
 * Product feature switches (client). Import where a flow might call AI or show AI UI.
 * Server: keep AI-related Cloud Functions undeployed / disabled separately.
 */
export const AI_FEATURES_ENABLED = false;
